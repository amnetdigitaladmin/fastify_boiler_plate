name: Publish and Update Consumers

on:
  push:
    branches: [main]
    paths:
      - 'shared_models/**' # Only trigger when shared_models folder changes

jobs:
  publish-and-update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          registry-url: 'https://registry.npmjs.org/'

      - name: Install dependencies
        run: |
          cd shared_models
          ls
          npm ci

      - name: Build shared models
        run: |
          cd shared_models
          npm run build

      - name: Publish to npm
        run: |
          cd shared_models
          npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Wait for npm propagation
        run: sleep 15

      - name: Get latest version from npm
        id: latest
        run: |
          LATEST=$(npm show @rohithkumar/shared_models version)
          echo "LATEST_VERSION=$LATEST" >> $GITHUB_ENV
          echo "Latest version is $LATEST"

      - name: Clone app repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_PAT }}@github.com/amnetdigitaladmin/fastify_boiler_plate.git
          cd fastify_boiler_plate/Fastify-app
          sed -i "s|\"@rohithkumar/shared_models\": \".*\"|\"@rohithkumar/shared_models\": \"^${{ env.LATEST_VERSION }}\"|" package.json
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add package.json
          git commit -m "chore: update shared_models to v${{ env.LATEST_VERSION }}" || echo "No changes to commit"
          git push origin main

